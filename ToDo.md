# TODO

- Optimize data structure for KLineFeed. It should be easily hold multiple symbols for looping over the symbols.
- Create reporting class. Move the logic from engine to reporting. Also move the tqdm like progress to reporting class.
- GUI implementation. For phase 1, simple dashboard like is enough. Maybe we could add "name()" requirement to strategy too.
- Review the order logic.